"use client"

import React from "react"
import PlayerRegistrationClient from "@/components/PlayerRegistrationClient"

// Simple client wrapper that renders the client registration UI. This
// file remains for compatibility if other parts import it.
export default function PlayerRegistrationClientWrapper() {
  return <PlayerRegistrationClient />
}
